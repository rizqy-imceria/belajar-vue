import IndexPage from '../index.vue'
console.log(IndexPage.name)

describe('test aja', () => {
  it('1 + 1 = 2', () => {
    expect(1 + 1).toBe(2)
  })

  // it('test nama index page', () => {
  //   expect(IndexPage.name).toBe('index')
  // })
})
